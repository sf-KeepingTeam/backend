# loadgen EC2 관측 스택

# 구성 1 (합침)
ALLINONE_HOST=10.0.1.200

# 구성 2 (나눔)
MONOLITH_HOST=10.0.1.100
QR_SERVICE_HOST=10.0.2.100

# Kafka (기본값은 ALLINONE_HOST 와 같다 — allinone 을 Kafka 로 전환하므로)
KAFKA_HOST=10.0.1.200

GRAFANA_ADMIN_PASSWORD=change_me
