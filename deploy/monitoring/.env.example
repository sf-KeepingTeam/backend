# loadgen EC2 관측 스택

# 구성 1 (합침)
ALLINONE_HOST=10.0.1.200

# 구성 2 (나눔)
MONOLITH_HOST=10.0.1.100
QR_SERVICE_HOST=10.0.2.100

# Kafka — 전용 5번째 EC2(keeping-kafka)의 Private IP.
#   미설정이면 127.0.0.1 로 폴백해서 target down 으로만 뜬다(설정 파싱은 통과).
KAFKA_HOST=10.0.3.100

GRAFANA_ADMIN_PASSWORD=change_me
