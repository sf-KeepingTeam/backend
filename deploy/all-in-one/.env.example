# 구성 1 (합침) — keeping-main EC2 한 대에서 사용
# 값은 deploy/monolith/.env, deploy/qr-service/.env 와 동일해야 한다.
# 다른 값을 쓰면 구성 1 vs 구성 2 비교가 오염된다.

DOCKER_USERNAME=keeping
MYSQL_ROOT_PASSWORD=change_me
JWT_SECRET=change_me
INTERNAL_AUTH_TOKEN=change_me
FE_BASE_URL=http://localhost:3030
QR_WEBHOOK_ENABLED=true

# 부하테스트 (측정 종료 후 false 로 원복)
SPRING_PROFILES_ACTIVE=prod,loadtest
QR_SPRING_PROFILES_ACTIVE=docker
LOADTEST_BACKDOOR_ENABLED=false

# 캐시
CACHE_MODE=WRITE_THROUGH
CACHE_WARMING_ENABLED=true

# 측정 통제변인 — 구성 2와 반드시 같은 값으로 맞출 것
SERVER_TOMCAT_THREADS_MAX=200
SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE=20
HIKARI_MAX_POOL_SIZE=15
SPRING_JPA_HIBERNATE_DDL_AUTO=validate
