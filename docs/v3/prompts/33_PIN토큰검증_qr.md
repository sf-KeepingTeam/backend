# 세트 #33 — PIN 토큰 검증 경로 (qr-service)

> 아래 전체를 새 대화창에 그대로 붙여넣으세요.
> **선행: 세트 #31(트랜잭션 경계) + 세트 #32(monolith 토큰 발급). 둘 다 끝나 있어야 한다.**
> **먼저 `docs/v3/worklog/32_PIN토큰발급.md` 의 "qr이 검증할 때 필요한 계약" 을 읽어라.**

---

## ⛔ 0. 절대 제약 — 코드 작성만 한다 (다른 모든 지시보다 우선)

| 금지 | |
|---|---|
| **빌드 실행** | **네가** `gradlew`·`docker build`·`docker compose` 를 부르는 것 금지 (훅이 자동으로 도는 건 예외 — 바로 아래 참조) |
| **테스트 실행** | 단위·통합·컨트랙트 **돌리지 마라.** Testcontainers가 Docker를 깨워 세션이 멎는다 |
| **서버 접근** | EC2, k6, Prometheus, DB 접속 금지 |
| **측정** | 성능 검증을 시도하지 마라 |
| **`git` 쓰기** | 커밋·브랜치·푸시 금지. `.git/` 수정은 권한 프롬프트를 띄운다 |

**검증은 인프라 담당이 재측정으로 한다.** 너는 **파일 편집만** 하고 끝낸다.
테스트 *코드*를 새로 쓰는 것은 괜찮다 — **실행하지만 마라.**

허용: 저장소 안 파일 읽기/쓰기, `grep` `rg` `find` `cat` `sed` `wc`,
`git --no-pager log/blame/diff/status` (읽기 전용).

**멈추지 마라.** 사람에게 질문하고 기다리지 마라. 판단이 갈리면 **가장 안전한 쪽으로 쓰고
그 가정을 워크로그에 명시**하라. 확인할 수 없는 것은 "확인 불가"로 적고 넘어가라.

**컴파일을 못 돌린다.** 그래서 반드시:
- 참조하는 클래스·메서드는 **실제 파일을 열어 시그니처를 확인**하고 쓴다. 기억으로 쓰지 마라
- `import` 를 빠짐없이, 정확한 패키지 경로로
- 새로 만든 필드·메서드를 쓰는 쪽과 만드는 쪽이 **같은 이름**인지 저장 직전에 재대조
- `@RequiredArgsConstructor` 에 의존하는 주입은 `private final` 선언을 빠뜨리지 않는다


### ⚠️ 훅이 자동으로 컴파일한다 — 네가 한 게 아니다

이 저장소에는 **`PostToolUse` 훅**이 걸려 있다 (`.claude/settings.json`, `.claude/settings.local.json`).
**`.java` 파일을 편집할 때마다 훅이 자동으로 `./gradlew compileJava` 를 돌린다.**

```
.claude/hooks/post-edit.sh    .java 편집 시 해당 모듈 compileJava (timeout 90s, 실패 시 exit 1)
.claude/hooks/smoke-build.sh  같은 일 (항상 exit 0)
.claude/hooks/check-env.sh    .env / application*.yml 저장 시 필수 변수 경고
```

**그래서 이런 출력이 보인다:**

```
🔨 [qr-service] Java 파일 변경 감지 → 컴파일 체크 중...
✅ [qr-service] 컴파일 성공
```

**이건 네가 빌드를 실행한 게 아니다. 훅이 한 것이고 §0 위반이 아니다.**

| 해야 할 것 | 하지 말 것 |
|---|---|
| 훅이 **컴파일 실패**를 뱉으면 **즉시 고쳐라.** 이게 이번 작업의 유일한 컴파일 안전망이다 | 훅이 돌았다고 해서 **너도 `./gradlew` 를 직접 불러도 된다고 착각하지 마라** |
| 훅 출력을 워크로그에 "훅 컴파일 결과"로 인용해도 된다 | **`./gradlew test` 는 절대 금지.** 훅은 `compileJava` 만 돌린다. 테스트는 Testcontainers(Docker)를 깨워서 세션이 멎는다 |
| 편집 후 훅 출력을 **반드시 확인하고 넘어가라** | 훅 결과를 "테스트 통과"로 부풀리지 마라. **컴파일과 테스트는 다르다** |

**훅 때문에 편집 하나마다 수 초가 붙는다.** 조급해하지 말고, 대신 **불필요한 재편집을 줄여라** —
한 파일을 여러 번 찔끔찔끔 고치지 말고 한 번에 제대로 써라.

---

## 1. 배경 — 왜 이걸 고치나 (30초)

Keeping은 선결제 포인트 결제 플랫폼이다. monolith(8080) + qr-service(8081), 별도 DB·Redis,
REST + Webhook(`X-Internal-Auth`) 통신.

부하측정으로 확정된 것:

| | 구성 1 (합침) | 구성 2 (분리) |
|---|---|---|
| 조회 부하 500 하 결제 성공률 | 31.7% | **99.1%** |
| approve p95 | 7,700 ms | **6,687 ms** |

**서버 분리는 "붕괴"는 막았으나 "지연"은 못 막았다.** SLO(1,000 ms)의 6.7배다.

**병목은 MySQL이다** — Hikari 풀을 20→60으로 3배 늘렸는데 처리량이 **−3.2%** 였고
CPU는 평균 42%로 여유였다. 커넥션이 부족했던 게 아니라 **커넥션 뒤의 MySQL이 한계**였다.

그래서 방향은 둘뿐이다: **쓰기를 줄인다 / 커넥션 점유 시간을 줄인다.**

상세 근거·수치: `docs/v3/요청_코드개선_측정근거.md`
전체 측정 기록: `docs/측정결과_부하격리/result.md`

---

## 2. 공유 계약 (임의 변경 금지 — 원본은 `docs/v3/오케스트레이션.md` §4)

### V-1. 설정 키

```yaml
payment:
  notification:
    async: true
  approve:
    split-transaction: true
  pin:
    token-enabled: true
    token-ttl-seconds: 60
```

`@ConfigurationProperties(prefix = "payment")` 로 **`PaymentTuningProperties` 하나**에 묶는다.
`@Value` 를 흩뿌리지 마라. **세트 #30이 이 클래스를 만들고 #31·#33이 필드를 추가한다.**

**플래그를 두는 이유**: 인프라 원칙이 "한 번에 하나만 바꾼다"인데 개선이 3건이다.
플래그가 있으면 **배포는 한 번, 측정은 조건별로** 할 수 있고,
개선이 오히려 악화시키면 **코드 롤백 없이 플래그 한 줄로 되돌린다.**
기본값은 전부 `true`.

### V-2. `PaymentStatus` enum 을 건드리지 않는다

새 중간 상태(`PROCESSING`/`AUTHORIZING` 등)를 **추가하지 마라.**
DB ENUM 컬럼이라 마이그레이션이 따라오고, **복구 스케줄러가 이미 오래된 `PENDING` 을
스윕하므로 필요가 없다.**

### V-3. 트랜잭션 경계 규약

```
[TX-A]  멱등 슬롯 선점 · intent 검증 · items 스냅샷 로드     → 커밋
[NO-TX] PIN 검증 (HTTP) · 자금 캡처 (HTTP)                  ← 커넥션 안 쥔다
[TX-B]  intent 재조회 · 상태 전이 · 멱등 complete            → 커밋
[AFTER] 알림
```

1. **`[NO-TX]` 구간에서 어떤 리포지토리도 호출하지 마라.** 하나라도 부르면 커넥션을 다시 쥔다
2. **TX-B는 intent를 재조회한다** — `PaymentIntent` 에 `@Version` 낙관적 락이 있다
3. **TX-A/TX-B는 별도 빈의 `REQUIRES_NEW`** — self-invocation은 프록시를 우회한다
4. **`IntentStatusUpdater` 패턴을 따르라.** 이미 있는 것을 새로 만들지 마라

### V-4. 관측 — 인프라가 재측정 판정에 쓴다. 바꾸지 마라

| 지표 | 기대 |
|---|---|
| `hikaricp_connections_active{qr}` 최대 | 15 → **15 미만** |
| `hikaricp_connections_pending{qr}` 최대 | 12 → **0** |
| `/actuator/health` | 424 ms → **수십 ms** ← **가장 명확한 지표** |

```
log.info("[APPROVE_PHASE] phase={} intentId={} elapsedMs={}", ...)
log.warn("[APPROVE_ORPHAN] intentId={} reason={}", ...)
```

### V-5. 하지 말아야 할 것 (측정으로 배제됨)

| | 이유 |
|---|---|
| **Argon2 파라미터 약화** | OWASP 1순위. 알고리즘 선택은 옳다. 측정용 도구였고 원복됐다 |
| **Hikari 풀만 키우기** | 20→60 실험에서 처리량 **−3.2%**. 병목이 아니다 |
| **톰캣 스레드만 키우기** | 200 포화는 원인이 아니라 결과다 |
| **캐싱 확대** | HTTP 호출은 없애도 같은 MySQL·CPU 경합은 못 막는다 |
| **PIN 잠금 정책 완화** | PIN은 6자리라 경우의 수가 10⁶. 실질 1차 방어선은 해시가 아니라 잠금이다 |

### V-6. 산출물 3종 (전부 파일)

1. 코드·테스트 파일 수정 (테스트는 **작성만**)
2. `docs/v3/worklog/<세트번호>_<이름>.md`
3. `docs/v3/worklog/인프라_인계.md` 에 **append** — 인프라가 재측정 전에 알아야 할 것
   (플래그 기본값·배포 순서 제약·새 설정·관측 지점). **부실하면 재측정이 막힌다**

---

## 3. 네 소유 경계

**소유**: `qr-service/src/main/java/com/ssafy/keeping/qr/**`, `qr-service/src/main/resources/application*.yml`

**소유 아님**: `monolith/**`(세트 #32) · `docs/v1/**` · `k6/` `deploy/` `monitoring/`

---

## 4. 고칠 것

### 4-1. 목표

`approve()` 가 monolith를 **3번** 부른다. 그중 PIN 검증이 가장 비싸다
(조회 부하 하 **627.3 ms**, approve의 **29%**).

```
현재:  approve → [PIN 검증 HTTP] → [자금 캡처 HTTP] → [알림 HTTP]
목표:  approve → [토큰 로컬 검증] → [자금 캡처 HTTP]        (알림은 세트 #30이 이미 뺐다)
```

**monolith 왕복 3회 → 2회.** 세트 #31이 이미 HTTP를 트랜잭션 밖으로 뺐으므로,
이번엔 **HTTP 자체를 하나 없앤다.**

### 4-2. 흐름 (v2 설계안 §4)

```
1~3. (변경 없음) QR 스캔 → 주문서 생성 → 금액 표시
4. 손님이 PIN 입력 → 프론트가 qr이 아니라 monolith로 직접 전송
5. monolith가 PIN 대조 → 성공 시 단기 서명 토큰 발급        ← 세트 #32 (완료)
6. 프론트 → qr /approve (PIN 대신 토큰 첨부)
7. qr이 토큰 서명을 로컬 검증 → monolith 호출 0             ← 네 작업
8. qr → monolith 차감 ← 이것만 남음
```

**두 서비스가 이미 `JWT_SECRET` 을 공유한다.** qr이 monolith 발급 JWT를 이미 검증하고 있으니
(`security/JwtAuthenticationFilter`) 그 인프라를 재사용하라. **새로 만들지 마라.**

### 4-3. 반드시 지킬 것

| # | | 왜 |
|---|---|---|
| 1 | **토큰의 intent publicId가 요청 경로의 것과 일치하는지 검증** | 없으면 A주문서용 토큰으로 B주문서를 결제할 수 있다. **이게 가장 중요한 검증이다** |
| 2 | **1회용 처리 — `jti` 를 Redis에 기록해 재사용 차단** | 멱등키가 이중결제는 이미 막지만 토큰 자체도 1회용이어야 깔끔하다. TTL은 토큰 수명보다 조금 길게 |
| 3 | **만료 검증** | `exp` 확인. 기본 60초 |
| 4 | **서명 검증** | `JWT_SECRET` 으로 |
| 5 | **`jti` 기록은 Redis 왕복 1회** | DB가 아니다. **트랜잭션 밖(`[NO-TX]` 구간)에서 해라** — 세트 #31의 규약을 깨지 마라 |

### 4-4. ⚠️ 병행 지원이 필수다 — 프론트가 이 저장소 밖에 있다

프론트는 `C:/keeping/frontend` 로 이 저장소 밖이다. **프론트 배포 전에 백엔드만 바뀌면 결제가 끊긴다.**

**→ `ApproveRequest` 가 PIN과 토큰을 둘 다 받게 하라.**

```
토큰이 있으면  → 로컬 검증 (새 경로)
토큰이 없으면  → 기존 PIN 검증 HTTP (기존 경로)
```

`payment.pin.token-enabled=false` 면 **토큰이 와도 기존 PIN 경로로** 처리한다 (V-1).
인프라가 before/after를 같은 빌드로 재기 위한 것이다.

**기존 `customerClient.verifyPin` 을 지우지 마라.**

### 4-5. 세트 #31과의 접점 — 규약을 깨지 마라

세트 #31이 만든 구조:

```
[TX-A]  멱등 슬롯 · intent 검증 · items 스냅샷     → 커밋
[NO-TX] PIN 검증 (HTTP) · 자금 캡처 (HTTP)        ← 커넥션 안 쥔다
[TX-B]  intent 재조회 · 상태 전이 · 멱등 complete  → 커밋
```

**토큰 검증은 `[NO-TX]` 구간에 들어간다.** 그리고:

- **토큰 검증 자체는 순수 계산이다** (서명 + 클레임). DB도 네트워크도 안 탄다
- **`jti` Redis 기록만 I/O다.** Redis는 Hikari 커넥션과 무관하니 규약 위반이 아니다
- **`[NO-TX]` 구간에서 리포지토리(JPA)를 부르지 마라.** 이건 여전히 금지다

**토큰 검증 실패 시 처리**를 세트 #31의 상태 전이 설계와 맞춰라 —
기존 PIN 실패와 같은 경로(`markDeclined`)로 갈지 다르게 갈지 결정하고 근거를 남겨라.

### 4-6. 정직하게 인정할 것

**토큰화를 해도 monolith가 죽으면 결제는 여전히 안 된다.** 자금 캡처가 남아 있다.

토큰화가 없애는 것은 **"qr 스레드가 monolith를 기다리는 시간"** 이지 **monolith 의존 자체가 아니다.**
워크로그와 문서에 이 구분을 명확히 써라. 부풀리지 마라.

---

## 5. 완료 기준 — "코드가 이렇게 바뀌었다"

- [ ] 토큰 경로에서 `customerClient.verifyPin` 호출이 **0건**
- [ ] **토큰의 intent publicId와 요청 경로의 것을 대조**한다
- [ ] `jti` 를 Redis에 기록해 **재사용을 차단**한다
- [ ] `exp` 만료 검증 + 서명 검증
- [ ] `ApproveRequest` 가 PIN·토큰 **둘 다** 받는다 (병행 지원)
- [ ] `payment.pin.token-enabled=false` 면 기존 PIN 경로로 되돌아감
- [ ] `customerClient.verifyPin` 이 **삭제되지 않았다**
- [ ] 토큰 검증이 세트 #31의 `[NO-TX]` 구간에 있고 **JPA 리포지토리를 부르지 않는다**

## 6. 테스트 (작성만. 실행 금지)

| # | 시나리오 | 단언 |
|---|---|---|
| K-1 | 유효한 토큰 | approve 성공 + **`customerClient.verifyPin` 이 호출되지 않음** (mock never) |
| K-2 | **다른 intent용 토큰** | **거부** ← 가장 중요한 테스트 |
| K-3 | 만료된 토큰 | 거부 |
| K-4 | 서명이 깨진 토큰 | 거부 |
| K-5 | **같은 토큰 2회** | 두 번째 거부 (`jti` 재사용 차단) |
| K-6 | 토큰 없이 PIN만 | 기존 경로로 동작 |
| K-7 | `token-enabled=false` + 토큰 있음 | 기존 PIN 경로로 동작 |
| K-8 | 토큰 검증 실패 시 intent 상태 | 세트 #31 설계와 일관되게 |

**K-2와 K-5가 이번 세트의 진짜 검증이다.** 토큰이 통과하는 것만 보면 의미가 없다.
**엉뚱한 토큰이 거부되는지**를 단언해야 한다.

## 7. 워크로그 — `docs/v3/worklog/33_PIN토큰검증.md`

```markdown
## 세트 #33 — PIN 토큰 검증 (qr)

### 세트 #32 계약과의 대조
| 클레임 | #32가 넣는다고 한 것 | #33이 읽는 것 | 일치 |
(32_PIN토큰발급.md 의 "qr이 검증할 때 필요한 계약" 을 그대로 옆에 놓고 대조하라)

### 검증 순서
1. 서명 → 2. exp → 3. intentPublicId 대조 → 4. jti 재사용 확인
(순서와 근거)

### jti 저장
- Redis 키 형식 / TTL / 왜 그 TTL인가:

### 병행 지원
- 분기 지점 (파일:줄):
- token-enabled=false 동작:

### 세트 #31 규약 준수
- 토큰 검증이 [NO-TX] 구간에 있음: (파일:줄)
- 그 구간 JPA 리포지토리 호출 0건 (grep 결과 붙여넣기):

### 변경 파일
### 계약 준수 / 컴파일 안전 자가점검
### 정직하게 — 토큰화가 없애는 것과 못 없애는 것
### 인프라_인계.md 에 추가한 항목
### 가정 / 확인하지 못한 것
```

## 8. 하지 말 것

1. **`customerClient.verifyPin` 을 지우지 마라.** 프론트가 저장소 밖이라 병행 지원이 필수다
2. **intent publicId 대조를 빠뜨리지 마라.** 이게 없으면 다른 주문서를 결제할 수 있다
3. **`jti` 를 DB에 기록하지 마라.** Redis다. MySQL이 병목인데 쓰기를 늘리면 개악이다
4. **`[NO-TX]` 구간에서 JPA 리포지토리를 부르지 마라.** 세트 #31의 성과가 사라진다
5. **새 시크릿·새 JWT 인프라를 만들지 마라.** `JWT_SECRET` 과 기존 검증 코드를 재사용하라
6. **`monolith/` 를 건드리지 마라**
7. **토큰 수명을 늘리지 마라.** 60초 내외다. 길수록 탈취 위험이 커진다
8. **"monolith 의존이 사라졌습니다"라고 쓰지 마라.** 자금 캡처가 남아 있다 (§4-6)
9. **`gradlew`·`docker`·`git` 쓰기를 실행하지 마라**
