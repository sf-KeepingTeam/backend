# 복귀 체크리스트 — v2 작업 1·2

> 무인 모드에서 코드 수정만 완료했다. 아래는 사람이 돌아와서 실행·확인할 것들이다.
> **순서대로 진행하라.** 앞 항목이 실패하면 뒤는 의미가 없다.

## 0. 가장 먼저 — 배포 순서 제약

- [ ] (#16) DDL 마이그레이션을 **단독으로 먼저** 적용. prod는 `ddl-auto=validate`라 컬럼 없이 #18 엔티티를 올리면 기동 실패. 컬럼이 nullable이라 먼저 올려두는 건 완전히 안전
  ```bash
  mysql -u root -p ssafy_fintech_db < mysql/migration/V2025_08_21__lot_expiry_columns.sql
  ```
- [ ] (#21→#22) qr restore 제거를 monolith restore 제거보다 먼저 배포

## 1. 컴파일 — 여기서 대부분 터질 것이다

- [ ] `(cd monolith && ./gradlew clean build -x test)` — 무인 모드에서 컴파일을 못 돌렸으므로 타입 오류가 남아 있을 수 있다
- [ ] `(cd qr-service && ./gradlew clean build -x test)`

## 2. 테스트

- [ ] Docker Desktop 실행 (Testcontainers가 요구)
- [ ] `(cd monolith && ./gradlew test)`
- [ ] `(cd qr-service && ./gradlew test)`

## 3. 확인 불가였던 사실 (전부 미확인)

- [ ] (#16) `mysql/ops/v2_스키마조사.sql` 실행 → 아래 3개 판정:
  - `transaction_type` ENUM에 `REFUND`가 실제로 있는가?
  - `customer_id`가 실제로 `NOT NULL`인가?
  - CHECK 제약이 실제로 살아 있는가? (MySQL 8.0.16+)
- [ ] (#16) 존량 불일치 건수 — `v2_스키마조사.sql`의 마지막 쿼리
- [ ] (#16) ALTER 소요 시간 — **측정하지 못했다.** ENUM MODIFY는 테이블 재구성 가능. 프로덕션 적용 전 반드시 테스트 환경에서 측정
- [ ] (#18) `FOR UPDATE SKIP LOCKED` 가 실서버 MySQL 버전에서 동작하는지 (8.0+ 필요)
- [ ] (#18) EXPLAIN 2종: 만료 배치 후보 조회 / 대사 쿼리
- [ ] (#20) prod 스키마 미확인 — customer 세팅은 양쪽 경우 모두 안전하도록 처리함

## 4. 실행 검증

- [ ] `curl -X POST -H "X-Internal-Auth: $TOKEN" localhost:8080/internal/ops/lot-expiry/run`
- [ ] `curl -X POST -H "X-Internal-Auth: $TOKEN" localhost:8080/internal/ops/reconcile/run`
- [ ] 헤더 없이 같은 호출 → 401 확인
- [ ] `curl localhost:8080/actuator/prometheus | grep wallet_` → 7개 메트릭 노출, Gauge가 NaN 아님
- [ ] `grep '\[LEDGER_MISMATCH\]' <로그>` → 클린 DB 기준 0건
- [ ] loadtest 프로필(로그 WARN)에서 `[LEDGER_MISMATCH]`가 보이는지 확인
- [ ] 데드락: `SHOW ENGINE INNODB STATUS`의 `LATEST DETECTED DEADLOCK`
- [ ] (#21) `restore` 엔드포인트 호출 → 404 확인 (양쪽 서비스)

## 5. 성능 (측정 못 함)

- [ ] (#23) lazy on/off 로 `capture` 응답시간 비교 (`wallet.expiry.lazy-enabled` 토글)
- [ ] 악화 시 후퇴안: lazy off + 만료 배치를 매시로 (설계안 §3-2). **설정만으로 가능해야 함**

## 6. [결정 필요] — 사람만 판단할 수 있는 것

- [ ] SettlementScheduler 처분 — 삭제 / 살림 / 주석에 이유 명시. 조사 결과: `@Service` 주석 처리됨, 빈 미등록, 정산 도메인 전체가 비활성 상태. 상세: `00_cleanup_scan.md`
- [ ] cleanup B등급 목록 개별 판단 (Wave 6 재스캔 필요)
- [ ] 존량 불일치 보정:
  - `balance > SUM(lot)`: **함부로 깎지 말 것.** 원인 규명 먼저. 사람 승인 필수
  - `balance < SUM(lot)`: 올려주는 건 상대적으로 안전
- [ ] `strict-lot=true` 전환 시점 — **대사에서 불일치 0을 실측한 뒤에만**
- [ ] 배포 전 프론트/운영 공유: 만료 포인트로 결제 시 "잔액 부족"으로 바뀜 (버그 수정이지만 CS 문의 가능)

## 7. 프로필별 검증

- [ ] `prod` 프로필 컨텍스트 로드 정상
- [ ] `loadtest` 프로필 컨텍스트 로드 정상
- [ ] `perf` 프로필 컨텍스트 로드 정상
- [ ] Gradle이 shedlock 의존성 2개를 실제로 받아오는지 (무인 모드에서 네트워크 다운로드를 못 해봤다)
